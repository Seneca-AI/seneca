syntax = "proto3";


// Primary Key: (id)
// Stored in DB.
message User {
    string id = 1;
    string email = 2;
    string phone_number = 3;
    // Milliseconds from epoch.
    int64 birthdate = 4;
    repeated string vehicle_id = 5;
    repeated string camera_id = 6;
}